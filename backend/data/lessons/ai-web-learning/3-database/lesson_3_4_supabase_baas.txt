# Урок 3.4: Backend-as-a-Service (Supabase) - PostgreSQL + Auth + Storage из коробки

> **Модуль 3:** Базы данных  
> **Урок:** 3.4  
> **Длительность:** 45-60 минут  
> **Prerequisite:** Урок 3.3 (Миграции и схема БД)

---

## 🎯 Цели урока

После этого урока ты сможешь:
- ✅ Понимать что такое Backend-as-a-Service (BaaS)
- ✅ Объяснить преимущества и недостатки BaaS vs собственный backend
- ✅ Использовать Supabase для PostgreSQL + Auth + Storage
- ✅ Выбирать между Supabase и собственным backend для разных проектов
- ✅ Настраивать Supabase через Claude Code
- ✅ Понимать альтернативы (Firebase, AWS Amplify, PocketBase)

---

## 📖 Концепция: Что такое Backend-as-a-Service?

### Простое определение

**Backend-as-a-Service (BaaS)** - это готовый backend "из коробки" с базой данных, аутентификацией, хранилищем файлов и API. Ты не пишешь backend код - всё уже работает, просто подключаешься.

**Собственный backend:**
```python
# Ты пишешь весь код руками:
# - FastAPI endpoints
# - SQLAlchemy модели
# - Auth система (JWT, токены)
# - File upload логика
# - Real-time WebSocket
# = 1000+ строк кода
```

**BaaS (Supabase):**
```typescript
// Всё уже готово, просто используешь:
const { data } = await supabase
  .from('projects')
  .select('*')
  
// = 3 строки кода
```

### Зачем нужен BaaS

**Проблемы собственного backend:**
- ❌ Нужно писать много boilerplate кода
- ❌ Настройка аутентификации сложная (JWT, sessions, OAuth)
- ❌ File storage нужно настраивать (S3, локальные файлы)
- ❌ Real-time нужно реализовывать (WebSocket)
- ❌ Нужно поддерживать сервер (deploy, мониторинг)

**С BaaS (Supabase):**
- ✅ База данных готова (PostgreSQL)
- ✅ Auth из коробки (email, OAuth, magic links)
- ✅ Storage для файлов (S3-подобный)
- ✅ Real-time подписки на изменения
- ✅ Бесплатный hosting (до определённых лимитов)

**BaaS = экономия времени и фокус на бизнес-логике!**

---

## 🏪 Аналогия: Ресторан vs Готовая еда

### Собственный backend = Ресторан с нуля

```
🏗️ СТРОИШЬ РЕСТОРАН С НУЛЯ

1. Найти помещение (сервер)
2. Купить оборудование (database, auth)
3. Нанять поваров (написать код)
4. Создать меню (API endpoints)
5. Настроить кухню (настройки)
6. Открыть ресторан (deploy)
7. Поддерживать работу (мониторинг)

Плюсы:
✅ Полный контроль
✅ Можешь делать ЧТО УГОДНО
✅ Своя кухня, свои правила

Минусы:
❌ Дорого (время + деньги)
❌ Долго (месяцы настройки)
❌ Сложно (нужна экспертиза)
```

---

### BaaS (Supabase) = Доставка готовой еды

```
🍱 ЗАКАЗЫВАЕШЬ ГОТОВУЮ ЕДУ

1. Открыл сайт (supabase.com)
2. Выбрал блюдо (создал проект)
3. Получил еду (БД готова)
4. Начал есть (подключился)

Плюсы:
✅ Быстро (5 минут setup)
✅ Дёшево ($0-25/месяц)
✅ Просто (без экспертизы)
✅ Всё готово (БД, Auth, Storage)

Минусы:
⚠️ Ограничения меню (нет экзотики)
⚠️ Зависимость от сервиса
⚠️ Цены растут при масштабе
```

**Supabase = "Доставка backend" вместо "Стройки ресторана"!**

---

## 💡 Контекст применения (КРИТИЧНО!)

### Когда использовать BaaS (Supabase)

**✅ Идеально для:**

1. **MVP и прототипы**
   - Быстрый запуск (часы, а не недели)
   - Минимальные затраты ($0)
   - Фокус на продукте, а не на инфраструктуре

2. **Малые и средние проекты**
   - До 10,000 активных пользователей
   - Стандартная функциональность (CRUD, Auth)
   - Ограниченный бюджет

3. **Solo developer / Стартапы**
   - Один разработчик
   - Нет backend экспертизы
   - Нужно быстро валидировать идею

4. **Проекты с real-time**
   - Чаты
   - Коллаборативные приложения
   - Dashboards с live данными

**Примеры:**
- Todo приложение
- Блог с комментариями
- Simple SaaS (dashboard для проектов)
- E-commerce (небольшой)
- Portfolio с CMS

---

### Когда НЕ использовать BaaS (нужен собственный backend)

**⚠️ Собственный backend лучше для:**

1. **Сложная бизнес-логика**
   - Математические расчёты (как EngineCamPro)
   - ML/AI интеграции
   - Специфичные алгоритмы
   - Нестандартная обработка данных

2. **Высокая нагрузка**
   - 100,000+ активных пользователей
   - Миллионы запросов в день
   - Критическая производительность

3. **Специфичные требования**
   - Кастомная аутентификация
   - Нестандартные протоколы
   - Legacy системы
   - Compliance (HIPAA, GDPR строго)

4. **Полный контроль критичен**
   - Банковские приложения
   - Healthcare системы
   - Правительственные проекты

**Примеры:**
- EngineCamPro v2 (математика, NumPy, SciPy)
- Финтех приложения (trading, платежи)
- Медицинские системы
- Big data платформы

---

## 🔧 Три типа проектов курса - выбор Backend

### 1. EngineCamPro v2 (FastAPI + React)

**Используем: Собственный FastAPI backend + Supabase PostgreSQL** 🔄

**Почему НЕ полностью Supabase:**
```python
# EngineCamPro нужна сложная математика:
import numpy as np
from scipy import interpolate

def calculate_cam_profile(params):
    # Сложные расчёты профиля кулачка
    # NumPy, SciPy, matplotlib
    # = Python Backend обязателен!
    ...
```

**Что берём от Supabase:**
- ✅ PostgreSQL база данных (бесплатный hosting)
- ✅ Auth для пользователей (можно, но не обязательно)
- ✅ Storage для экспорта файлов (опционально)

**Что пишем сами:**
- ✅ FastAPI для математических расчётов
- ✅ Endpoints для API
- ✅ Бизнес-логика

**Архитектура:**
```
React Frontend
    ↓
FastAPI Backend (математика)
    ↓
Supabase PostgreSQL (только БД)
```

**Вывод:** Гибридный подход - backend свой, БД от Supabase.

---

### 2. Быстрые Full-Stack проекты (Next.js)

**Используем: Supabase полностью** ✅

**Почему Supabase идеален:**
```typescript
// Простой проект без сложной логики:
// - Блог
// - Portfolio
// - Landing page
// - Simple dashboard

// Supabase даёт ВСЁ что нужно:
const { data: posts } = await supabase
  .from('posts')
  .select('*')
  .order('created_at', { ascending: false })

// Auth из коробки:
const { user } = await supabase.auth.signUp({
  email, password
})

// File upload:
await supabase.storage
  .from('avatars')
  .upload('avatar.jpg', file)
```

**Что НЕ нужно:**
- ❌ Писать backend на Python/Node.js
- ❌ Настраивать Auth систему
- ❌ Деплоить backend сервер

**Архитектура:**
```
Next.js Frontend
    ↓
Supabase (всё в одном)
├── PostgreSQL
├── Auth
├── Storage
└── Real-time
```

**Вывод:** Supabase полностью заменяет backend!

---

### 3. Интерактивная онлайн-школа (RAG + AI)

**Используем: FastAPI backend + Supabase PostgreSQL + pgvector** 🔄

**Почему нужен backend:**
```python
# RAG система требует Python:
from langchain import OpenAI, VectorStore
from openai import OpenAI

# Сложная логика AI:
# - Embeddings
# - Vector search
# - LangChain orchestration
# = Python Backend обязателен!
```

**Что берём от Supabase:**
- ✅ PostgreSQL + pgvector (векторная БД)
- ✅ Auth для студентов
- ✅ Storage для документов

**Что пишем сами:**
- ✅ FastAPI для RAG системы
- ✅ AI endpoints (LangChain + OpenAI)
- ✅ WebSocket для real-time чата

**Архитектура:**
```
Next.js Frontend
    ↓
FastAPI Backend (RAG + AI)
    ↓
Supabase PostgreSQL + pgvector
```

**Вывод:** Гибридный подход - backend свой, БД от Supabase.

---

## 🔄 Supabase vs Альтернативы

### Сравнительная таблица BaaS решений

| Аспект | Supabase | Firebase | AWS Amplify | PocketBase |
|--------|----------|----------|-------------|------------|
| **База данных** | PostgreSQL (SQL) | Firestore (NoSQL) | DynamoDB (NoSQL) | SQLite (SQL) |
| **Auth** | ✅ Отлично | ✅ Отлично | ✅ Отлично | ✅ Базовый |
| **Storage** | ✅ S3-подобный | ✅ Firebase Storage | ✅ S3 | ✅ Локальный |
| **Real-time** | ✅ PostgreSQL subscriptions | ✅ Firestore real-time | ⚠️ Через AppSync | ✅ Да |
| **Цена (бесплатный tier)** | 🟢 Щедрый | 🟢 Щедрый | 🟡 Ограниченный | 🟢 Бесплатный |
| **SQL vs NoSQL** | SQL | NoSQL | NoSQL | SQL |
| **Self-hosted** | ✅ Да (Docker) | ❌ Нет | ❌ Нет | ✅ Да |
| **Open source** | ✅ Да | ❌ Нет | ❌ Нет | ✅ Да |
| **Dashboard** | ✅ Отличный | ✅ Отличный | ⚠️ Сложный | ✅ Простой |
| **Для EngineCamPro** | ✅ PostgreSQL нужен | ❌ NoSQL не подходит | ❌ NoSQL не подходит | ⚠️ SQLite слабо |
| **Для Next.js блога** | ✅ Отлично | ✅ Отлично | ⚠️ Сложно | ✅ Отлично |
| **Vendor lock-in** | ⚠️ Средний | ❌ Высокий | ❌ Высокий | ✅ Низкий |

### Когда использовать каждый?

#### 🟢 Supabase - выбери для:
- SQL база нужна (relationships, JOIN)
- PostgreSQL extensions (pgvector для RAG)
- Open source важен
- Хочешь self-host в будущем
- **👉 EngineCamPro v2 (PostgreSQL)**

#### 🔥 Firebase - выбери для:
- NoSQL база подходит (гибкая схема)
- Google ecosystem (Analytics, Cloud Functions)
- Mobile приложения (iOS/Android SDK отличные)
- Нужна максимальная простота

#### ☁️ AWS Amplify - выбери для:
- Уже используешь AWS
- Enterprise проект (масштаб)
- Нужны AWS сервисы (Lambda, S3, etc)
- Бюджет не проблема

#### 📦 PocketBase - выбери для:
- Хочешь self-host (свой сервер)
- Tiny проект (личный проект)
- Один файл backend (Go binary)
- Максимальная простота

---

## 🌐 Как работает Supabase

### Архитектура Supabase

```
┌─────────────────────────────────────────────────────────────────┐
│                        SUPABASE ПЛАТФОРМА                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  💻 YOUR APP              🌐 SUPABASE CLOUD                      │
│                                                                 │
│  Next.js/React            ┌─────────────────────────────────┐  │
│  Frontend                 │      🗄️ PostgreSQL               │  │
│      │                    │      (Ваша база данных)          │  │
│      │  REST API          │                                  │  │
│      └───────────────────►│  ┌────────────┐  ┌────────────┐  │  │
│                           │  │   users    │  │  projects  │  │  │
│      │  GraphQL (opt)     │  ├────────────┤  ├────────────┤  │  │
│      └───────────────────►│  │ id  email  │  │ id   name  │  │  │
│                           │  └────────────┘  └────────────┘  │  │
│      │  Realtime          │                                  │  │
│      └───────────────────►│      🔐 Auth (GoTrue)            │  │
│                           │      - Email/Password            │  │
│  FastAPI                  │      - OAuth (Google, GitHub)    │  │
│  Backend                  │      - Magic Links               │  │
│      │  REST API          │      - JWT tokens                │  │
│      └───────────────────►│                                  │  │
│                           │      📦 Storage (S3-like)         │  │
│                           │      - File upload               │  │
│                           │      - CDN delivery              │  │
│                           │      - Access control            │  │
│                           │                                  │  │
│                           │      📡 Realtime (WebSocket)     │  │
│                           │      - Subscribe to changes      │  │
│                           │      - Broadcast messages        │  │
│                           └─────────────────────────────────┘  │
│                                                                 │
│  🌍 DASHBOARD: https://app.supabase.com                        │
│     - Table Editor (like Excel)                                │
│     - SQL Editor (run queries)                                 │
│     - Auth management                                          │
│     - Storage browser                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Ключевые компоненты

**1. PostgreSQL Database**
- Полноценная PostgreSQL БД
- SQL queries, relationships, transactions
- Extensions (pgvector, PostGIS)
- Row Level Security (RLS) для безопасности

**2. Auth (GoTrue)**
- Email/Password аутентификация
- OAuth providers (Google, GitHub, Discord, etc)
- Magic links (passwordless)
- JWT tokens автоматически

**3. Storage**
- S3-подобное file storage
- CDN для быстрой доставки
- Access policies (кто может скачать)
- Thumbnails для изображений

**4. Realtime**
- WebSocket подписки на изменения БД
- Broadcast для custom events
- Presence для "кто онлайн"

**5. Edge Functions (опционально)**
- Serverless функции на Deno
- Запускаются близко к пользователю
- Для custom логики

---

## 📚 Supabase основы

### Установка Supabase Client

**Next.js / React:**
```bash
npm install @supabase/supabase-js
```

**Python (FastAPI):**
```bash
pip install supabase
```

### Подключение к Supabase

**JavaScript/TypeScript:**
```typescript
// lib/supabase.ts
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!

export const supabase = createClient(supabaseUrl, supabaseKey)
```

**Python:**
```python
# app/supabase.py
from supabase import create_client
import os

supabase_url = os.getenv("SUPABASE_URL")
supabase_key = os.getenv("SUPABASE_KEY")

supabase = create_client(supabase_url, supabase_key)
```

### CRUD операции через Supabase

**Create:**
```typescript
const { data, error } = await supabase
  .from('projects')
  .insert({ 
    name: 'My Project',
    user_id: user.id 
  })
  .select()
```

**Read:**
```typescript
const { data, error } = await supabase
  .from('projects')
  .select('*')
  .eq('user_id', user.id)
  .order('created_at', { ascending: false })
```

**Update:**
```typescript
const { data, error } = await supabase
  .from('projects')
  .update({ name: 'Updated Name' })
  .eq('id', projectId)
```

**Delete:**
```typescript
const { data, error } = await supabase
  .from('projects')
  .delete()
  .eq('id', projectId)
```

### Real-time подписки

```typescript
// Подписка на изменения в таблице projects
const subscription = supabase
  .channel('projects-channel')
  .on('postgres_changes', 
    { 
      event: '*', // INSERT, UPDATE, DELETE
      schema: 'public',
      table: 'projects' 
    },
    (payload) => {
      console.log('Change:', payload)
      // Обновить UI
    }
  )
  .subscribe()

// Отписаться
subscription.unsubscribe()
```

---

## 🤖 Работа с Claude Code

### Задача: Настроить Supabase для простого проекта

---

### ❌ Плохой диалог (неэффективный)

```
Ты: Настрой Supabase для проекта.

Claude Code: Какой проект? Что нужно настроить?

Ты: Веб-приложение.

Claude Code: Next.js? React? Какие данные хранить?

Ты: Да, что-то такое. Сделай стандартно.

Claude Code: [создаёт базовую настройку, может не подойти]
```

**Проблемы:**
- ❌ Нет контекста проекта
- ❌ Не указана структура данных
- ❌ Неясна архитектура
- ❌ Claude Code вынужден угадывать

---

### ✅ Хороший диалог (эффективный)

```
Ты: Настрой Supabase для простого Next.js проекта - Todo приложение.

Контекст проекта:
- Frontend: Next.js 15 (App Router)
- Backend: Supabase (полностью замена backend)
- Пользователи: Auth через email/password
- Функционал: Todo CRUD + real-time updates

Структура данных:

1. Таблица: todos
   - id: UUID (primary key, auto-generated)
   - user_id: UUID (FK к auth.users)
   - title: TEXT (not null)
   - completed: BOOLEAN (default false)
   - created_at: TIMESTAMP (auto)

Row Level Security (RLS):
- Пользователь видит только свои todos
- INSERT, UPDATE, DELETE только для owner

Требования:
1. Создать Supabase проект (я дам URL и keys)
2. Настроить Next.js integration:
   - lib/supabase.ts (client setup)
   - .env.local (environment variables)
3. Создать SQL миграцию для таблицы todos
4. Настроить RLS policies
5. Создать React hooks:
   - useTodos() - fetch и real-time
   - useAuth() - login/logout

Claude Code: Отлично! Детальный план настройки Supabase для Next.js Todo:

**Шаг 1: Supabase проект (ты создашь вручную)**

1. Зайди на https://app.supabase.com
2. Создай новый проект:
   - Название: "todo-app"
   - Database Password: [сохрани!]
   - Region: выбери ближайший
3. Дождись создания (~2 минуты)

4. Получи credentials:
   - Settings → API
   - Project URL → скопируй
   - Project API Key (anon public) → скопируй

**Шаг 2: Next.js setup**

Создам:

.env.local:
```
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
```

lib/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!

export const supabase = createClient(supabaseUrl, supabaseKey)

// Types
export type Todo = {
  id: string
  user_id: string
  title: string
  completed: boolean
  created_at: string
}
```

**Шаг 3: SQL миграция (таблица + RLS)**

Создам SQL файл:

migrations/001_create_todos.sql:
```sql
-- Создать таблицу todos
CREATE TABLE todos (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  title TEXT NOT NULL,
  completed BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Индекс для быстрого поиска по user_id
CREATE INDEX idx_todos_user_id ON todos(user_id);

-- Включить Row Level Security
ALTER TABLE todos ENABLE ROW LEVEL SECURITY;

-- Policy: Пользователь видит только свои todos
CREATE POLICY "Users can view their own todos"
  ON todos FOR SELECT
  USING (auth.uid() = user_id);

-- Policy: Пользователь может создавать свои todos
CREATE POLICY "Users can create their own todos"
  ON todos FOR INSERT
  WITH CHECK (auth.uid() = user_id);

-- Policy: Пользователь может обновлять свои todos
CREATE POLICY "Users can update their own todos"
  ON todos FOR UPDATE
  USING (auth.uid() = user_id);

-- Policy: Пользователь может удалять свои todos
CREATE POLICY "Users can delete their own todos"
  ON todos FOR DELETE
  USING (auth.uid() = user_id);
```

Применить миграцию:
1. Зайди в Supabase Dashboard → SQL Editor
2. Создай новый query
3. Вставь SQL из файла
4. Run query

**Шаг 4: React hooks**

Создам:

hooks/useTodos.ts:
```typescript
import { useEffect, useState } from 'react'
import { supabase, Todo } from '@/lib/supabase'

export function useTodos(userId: string | undefined) {
  const [todos, setTodos] = useState<Todo[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    if (!userId) return

    // Fetch todos
    fetchTodos()

    // Subscribe to real-time changes
    const subscription = supabase
      .channel('todos-channel')
      .on('postgres_changes',
        { 
          event: '*',
          schema: 'public',
          table: 'todos',
          filter: `user_id=eq.${userId}`
        },
        () => fetchTodos() // Reload on changes
      )
      .subscribe()

    return () => {
      subscription.unsubscribe()
    }
  }, [userId])

  async function fetchTodos() {
    const { data, error } = await supabase
      .from('todos')
      .select('*')
      .eq('user_id', userId)
      .order('created_at', { ascending: false })

    if (!error) setTodos(data)
    setLoading(false)
  }

  async function addTodo(title: string) {
    await supabase
      .from('todos')
      .insert({ title, user_id: userId })
  }

  async function toggleTodo(id: string, completed: boolean) {
    await supabase
      .from('todos')
      .update({ completed })
      .eq('id', id)
  }

  async function deleteTodo(id: string) {
    await supabase
      .from('todos')
      .delete()
      .eq('id', id)
  }

  return { 
    todos, 
    loading, 
    addTodo, 
    toggleTodo, 
    deleteTodo 
  }
}
```

hooks/useAuth.ts:
```typescript
import { useEffect, useState } from 'react'
import { supabase } from '@/lib/supabase'
import { User } from '@supabase/supabase-js'

export function useAuth() {
  const [user, setUser] = useState<User | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    // Get initial session
    supabase.auth.getSession().then(({ data: { session } }) => {
      setUser(session?.user ?? null)
      setLoading(false)
    })

    // Listen for auth changes
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setUser(session?.user ?? null)
      }
    )

    return () => subscription.unsubscribe()
  }, [])

  async function signUp(email: string, password: string) {
    const { error } = await supabase.auth.signUp({ email, password })
    return { error }
  }

  async function signIn(email: string, password: string) {
    const { error } = await supabase.auth.signInWithPassword({ 
      email, 
      password 
    })
    return { error }
  }

  async function signOut() {
    await supabase.auth.signOut()
  }

  return { user, loading, signUp, signIn, signOut }
}
```

**Шаг 5: Пример использования в компоненте**

app/page.tsx:
```typescript
'use client'
import { useAuth } from '@/hooks/useAuth'
import { useTodos } from '@/hooks/useTodos'

export default function Home() {
  const { user, signIn, signOut } = useAuth()
  const { todos, addTodo, toggleTodo, deleteTodo } = useTodos(user?.id)

  if (!user) {
    return <LoginForm onSignIn={signIn} />
  }

  return (
    <div>
      <h1>My Todos</h1>
      <button onClick={signOut}>Sign Out</button>
      
      <TodoList 
        todos={todos}
        onToggle={toggleTodo}
        onDelete={deleteTodo}
      />
      
      <AddTodoForm onAdd={addTodo} />
    </div>
  )
}
```

Готово! Supabase настроен. Теперь:
1. Создай Supabase проект
2. Скопируй credentials в .env.local
3. Примени SQL миграцию
4. npm install @supabase/supabase-js
5. npm run dev

Всё работает! Real-time updates из коробки. 🎉
```

**Почему это эффективно:**
- ✅ Детальный контекст проекта (Todo app)
- ✅ Чёткая структура данных (таблица todos)
- ✅ RLS policies для безопасности
- ✅ React hooks для удобства
- ✅ Real-time setup из коробки
- ✅ Claude Code создал полную интеграцию

---

## 🛠️ Практическое задание

### Задача

Изучи Supabase Dashboard и попробуй создать простой проект через диалог с Claude Code.

---

### Работа с Claude Code

**Шаг 1:** Создай Supabase проект

1. Зайди на [https://app.supabase.com](https://app.supabase.com)
2. Sign up (бесплатно)
3. Create New Project:
   - Name: `test-project`
   - Database Password: (запомни!)
   - Region: Europe West (London)
4. Wait for setup (~2 min)

---

**Шаг 2:** Изучи Dashboard

**Table Editor:**
- Создай таблицу вручную (кнопка "New table")
- Название: `notes`
- Columns: `id` (uuid), `title` (text), `content` (text)
- Добавь несколько записей вручную

**SQL Editor:**
- Попробуй запросы:
  ```sql
  SELECT * FROM notes;
  INSERT INTO notes (title, content) VALUES ('Test', 'Content');
  ```

**API Docs:**
- Settings → API
- Посмотри auto-generated REST API docs
- Попробуй curl запрос

---

**Шаг 3:** (Опционально) Настрой через Claude Code

Если хочешь попробовать интеграцию - попроси Claude Code создать простой Next.js проект с Supabase (как в диалоге выше).

---

## ❓ Вопросы для самопроверки

### 1. Объясни что такое BaaS через аналогию

Используй свою аналогию (не обязательно ресторан). Например: BaaS как...?

<details>
<summary>💡 Подсказка</summary>

Подумай про ситуации где есть "готовое решение":
- Квартира vs строительство дома
- Готовая одежда vs пошив на заказ
- Такси vs собственный автомобиль
- Аренда vs покупка
</details>

---

### 2. Когда использовать BaaS, когда собственный backend?

Приведи по 3 примера проектов для каждого варианта.

**BaaS (Supabase) подходит:**
1. _______
2. _______
3. _______

**Собственный backend нужен:**
1. _______
2. _______
3. _______

<details>
<summary>💡 Подсказка</summary>

BaaS: Todo app, блог, простой SaaS, MVP
Собственный: EngineCamPro, финтех, ML платформа, высокая нагрузка
</details>

---

### 3. Supabase vs Firebase - в чём разница?

Перечисли минимум 3 ключевых отличия.

<details>
<summary>💡 Подсказка</summary>

Подумай про:
- SQL vs NoSQL
- Open source vs Proprietary
- PostgreSQL vs Firestore
- Self-hosting возможность
</details>

---

### 4. Для EngineCamPro v2 - полностью Supabase или гибрид?

Объясни почему нельзя использовать только Supabase для EngineCamPro v2.

<details>
<summary>💡 Подсказка</summary>

Вспомни про:
- Математические расчёты (NumPy, SciPy)
- Python backend необходимость
- Что берём от Supabase (только PostgreSQL)
</details>

---

### 5. Row Level Security (RLS) - зачем?

Объясни своими словами что такое RLS и приведи пример policy.

<details>
<summary>💡 Подсказка</summary>

RLS = правила доступа на уровне строк
Пример: "Пользователь видит только свои проекты"
Policy: `WHERE user_id = auth.uid()`
</details>

---

## 🔗 Связь с другими уроками

**Основано на уроках:**
- **Урок 3.1:** SQL vs NoSQL - Supabase использует PostgreSQL (SQL)
- **Урок 3.2:** SQLAlchemy ORM - подключаемся к Supabase PostgreSQL
- **Урок 3.3:** Миграции - применяли миграции к Supabase БД

**Подготавливает к урокам:**
- **Модуль 4:** Frontend - будем подключать React к Supabase
- **Модуль 5:** Интеграция - FastAPI + Supabase PostgreSQL
- **Модуль 6:** Монетизация - возможно использование Supabase Auth

**Связь с EngineCamPro:**
- Используем Supabase ТОЛЬКО для PostgreSQL hosting
- Backend остаётся FastAPI (для математики)
- Гибридный подход: лучшее из двух миров

**Связь с Next.js (Модуль 8):**
- В Модуле 8 можем использовать Supabase ПОЛНОСТЬЮ
- Заменит backend для простых проектов

**Связь с RAG (Модуль 9):**
- Supabase PostgreSQL + pgvector extension для RAG

---

## ✅ Критерии завершения урока

**Понимание (могу объяснить):**
- [ ] Что такое Backend-as-a-Service (BaaS)
- [ ] Когда использовать BaaS vs собственный backend
- [ ] Возможности Supabase (PostgreSQL, Auth, Storage, Real-time)
- [ ] Row Level Security (RLS) и зачем он нужен
- [ ] Разница Supabase vs Firebase

**Практические навыки:**
- [ ] Создал Supabase проект в Dashboard
- [ ] Изучил Table Editor, SQL Editor, API Docs
- [ ] Понимаю как подключиться к Supabase из кода
- [ ] Могу выбрать между BaaS и собственным backend

**Готовность к следующему модулю:**
- [ ] Завершён Модуль 3 (все 4 урока) ✅
- [ ] Понимаю полный стек БД (SQL, ORM, Миграции, BaaS)
- [ ] Готов к Frontend разработке (Модуль 4)

**Дополнительно:**
- [ ] Задокументировал выводы в `notes/module-3/lesson-3-4-notes.md`
- [ ] Ответил на все вопросы для самопроверки

---

## 📚 Дополнительные материалы (для углубления)

Если хочешь узнать больше:

**Документация:**
- [Supabase официальная документация](https://supabase.com/docs) - полное руководство
- [Supabase Row Level Security](https://supabase.com/docs/guides/auth/row-level-security) - RLS гайд
- [Firebase vs Supabase](https://supabase.com/alternatives/supabase-vs-firebase) - сравнение

**Статьи:**
- "Backend-as-a-Service: The Future of Web Development"
- "When to Use BaaS vs Custom Backend"
- "Supabase Real-time: How It Works"

**Видео (опционально):**
- "Supabase in 100 Seconds" by Fireship
- "Build a Full Stack App with Next.js + Supabase"

**НО!** Это не обязательно для курса. Главное - понять концепцию BaaS и когда использовать Supabase.

---

**Статус урока:** ⏳ Не начат  
**Дата начала:** _________  
**Дата завершения:** _________  
**Время потрачено:** _______ минут

---

## 🎉 Поздравляю! Модуль 3 ЗАВЕРШЁН!

Ты прошёл весь **Модуль 3: Базы данных** и теперь понимаешь:

### Что ты изучил в Модуле 3:

✅ **Урок 3.1:** SQL vs NoSQL - выбор типа БД для разных проектов  
✅ **Урок 3.2:** PostgreSQL и SQLAlchemy ORM - работа с БД из Python  
✅ **Урок 3.3:** Миграции и схема БД - безопасная эволюция БД через Alembic  
✅ **Урок 3.4:** Backend-as-a-Service - когда использовать Supabase

### Для EngineCamPro v2 ты теперь можешь:

- ✅ Выбрать правильную БД (PostgreSQL)
- ✅ Создать модели через SQLAlchemy
- ✅ Управлять миграциями через Alembic
- ✅ Использовать Supabase для hosting PostgreSQL
- ✅ Применять лучшие практики работы с БД

---

## 🚀 Что дальше?

**Модуль 4: Frontend разработка** (6 уроков)

В следующем модуле ты изучишь:
- HTML/CSS/JavaScript основы
- React компоненты и hooks
- Визуализация данных (графики)
- UI библиотеки
- Связь Frontend с Backend

**Прогресс курса:**
```
████████░░░░░░░░░░░░░░░░░░░░ 27.6% (8/29 основных уроков)

✅ Модуль 1: Основы (4/4)
⏸️ Модуль 2: Backend (0/8)
✅ Модуль 3: БД (4/4) ← ЗАВЕРШЁН!
⏸️ Модуль 4: Frontend (0/6)
⏸️ Модуль 5: Интеграция (0/3)
⏸️ Модуль 6: Монетизация (0/2)
⏸️ Модуль 7: Deploy (0/2)
```

**Помни:** База данных - это фундамент приложения. Ты заложил прочный фундамент для EngineCamPro v2! 🎉🗄️
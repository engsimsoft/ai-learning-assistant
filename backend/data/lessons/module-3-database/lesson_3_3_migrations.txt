# Урок 3.3: Миграции и схема БД - Эволюция базы данных без потери данных

> **Модуль 3:** Базы данных  
> **Урок:** 3.3  
> **Длительность:** 50-65 минут  
> **Prerequisite:** Урок 3.2 (PostgreSQL и SQLAlchemy ORM)

---

## 🎯 Цели урока

После этого урока ты сможешь:
- ✅ Понимать что такое миграции БД и зачем они нужны
- ✅ Объяснить проблему изменения схемы БД без миграций (потеря данных!)
- ✅ Использовать Alembic для управления миграциями SQLAlchemy
- ✅ Создавать и применять миграции через Claude Code
- ✅ Понимать как откатывать изменения (rollback)
- ✅ Работать с миграциями в команде (Git + миграции)

---

## 📖 Концепция: Что такое миграции БД?

### Простое определение

**Миграция БД (Database Migration)** - это версионный контроль для схемы базы данных. Как Git для кода, но для структуры таблиц. Миграция = набор инструкций "как изменить схему БД" (добавить колонку, удалить таблицу, и т.д.).

**Без миграций:**
```sql
-- Ты вручную меняешь схему через SQL:
ALTER TABLE projects ADD COLUMN status VARCHAR(50);

-- Проблемы:
-- ❌ Коллега не знает об изменении
-- ❌ Production БД не обновлена
-- ❌ Невозможно откатить изменение
-- ❌ Нет истории изменений
```

**С миграциями (Alembic):**
```python
# Миграция создаётся автоматически:
alembic revision --autogenerate -m "Add status to projects"

# Применяется командой:
alembic upgrade head

# Можно откатить:
alembic downgrade -1

# История версий сохранена в Git
```

### Зачем нужны миграции

**Представь ситуацию:**
1. Ты создал таблицу `projects` с полями: `id`, `name`
2. В БД уже 100 проектов пользователей
3. Нужно добавить поле `description`
4. **Как это сделать БЕЗ потери 100 проектов?**

**Без миграций:**
```sql
-- ОПАСНО! Можно потерять данные
DROP TABLE projects;
CREATE TABLE projects (..., description TEXT);
-- ❌ Все 100 проектов удалены!
```

**С миграциями:**
```python
# Alembic создаёт миграцию:
# "Добавить колонку description к существующей таблице"
# ✅ Все 100 проектов сохранены!
```

**Миграции = безопасная эволюция БД без потери данных!**

---

## 🏪 Аналогия: Версии документа в Google Docs

Представь что ты пишешь документ в Google Docs с командой:

### Без версионного контроля (Без миграций)

```
Понедельник:
📄 Документ v1: "Отчёт 2025"
   - Глава 1: Введение
   - Глава 2: Анализ

Вторник:
Коллега открыл документ и добавил "Глава 3: Выводы"
❓ Ты не знаешь что добавлено!

Среда:
Ты случайно удалил Главу 2
❌ Как вернуть? История потеряна!

Четверг:
Нужно вернуть версию Понедельника
❌ Невозможно! Нет сохранённых версий
```

**Проблемы:**
- Нет истории изменений
- Невозможно откатиться
- Коллеги не синхронизированы

---

### С версионным контролем (С миграциями)

```
Понедельник:
📄 Version 1: "Отчёт 2025"
   - Глава 1: Введение
   - Глава 2: Анализ
   💾 Сохранено в истории

Вторник:
📄 Version 2: Коллега добавил "Глава 3: Выводы"
   ✅ Изменение записано: "Добавлена Глава 3 (Коллега, 2025-10-12)"
   ✅ Все видят что изменилось

Среда:
Ты случайно удалил Главу 2
   ✅ Видно в истории: "Удалена Глава 2 (Ты, 2025-10-13)"
   ✅ Можно откатить к Version 2

Четверг:
Нужно вернуть версию Понедельника
   ✅ Легко! Откатиться к Version 1
   ✅ Все изменения записаны
```

**Преимущества:**
- ✅ История всех изменений
- ✅ Можно откатиться к любой версии
- ✅ Команда синхронизирована
- ✅ Понятно кто и что менял

**Alembic для БД = Version History для Google Docs!**

---

## 💡 Контекст применения (КРИТИЧНО!)

### Когда использовать миграции БД

**✅ ВСЕГДА используй миграции для:**

1. **Production приложения**
   - БД с реальными пользователями
   - Нельзя потерять данные
   - Обязательно нужна история изменений

2. **Командная разработка**
   - Несколько разработчиков
   - Нужна синхронизация схемы БД
   - Git + миграции = все на одной версии

3. **Continuous Deployment**
   - Автоматический deploy
   - CI/CD pipeline применяет миграции
   - Нет ручного вмешательства

4. **Любое изменение схемы БД:**
   - Добавление колонки
   - Удаление таблицы
   - Изменение типа данных
   - Добавление индекса
   - Изменение Foreign Key

---

### Когда миграции НЕ критичны (но всё равно рекомендуются)

**⚠️ Можно обойтись без миграций:**

1. **Локальная разработка в начале проекта**
   - Только ты работаешь
   - БД пустая (нет важных данных)
   - Можно удалить БД и создать заново

2. **Прототипы и MVP**
   - Схема меняется каждый час
   - Нет реальных пользователей
   - Можно дропнуть БД

**НО!** Как только есть реальные данные → миграции обязательны.

---

## 🔧 Три типа проектов курса - использование миграций

### 1. EngineCamPro v2 (FastAPI + React)

**Используем: Alembic** ✅

**Почему:**
```
Development → Staging → Production

Схема БД эволюционирует:
Version 1: Users, Projects, Cams (базовые поля)
Version 2: Добавили поле Projects.status
Version 3: Добавили индекс на Cams.cam_type
Version 4: Добавили таблицу Subscriptions

Миграции позволяют:
✅ Безопасно обновлять Production БД
✅ Откатиться если что-то пошло не так
✅ Команда работает на одной версии схемы
✅ CI/CD автоматически применяет миграции
```

**Workflow:**
```bash
# 1. Изменил модель в models/project.py
class Project(Base):
    status = Column(String(50))  # Новое поле!

# 2. Создал миграцию автоматически
alembic revision --autogenerate -m "Add status to projects"

# 3. Проверил миграцию
cat alembic/versions/xxx_add_status.py

# 4. Применил локально
alembic upgrade head

# 5. Закоммитил в Git
git add alembic/versions/xxx_add_status.py
git commit -m "Add project status field"

# 6. Deploy на production - миграция применится автоматически!
```

---

### 2. Быстрые Full-Stack проекты (Next.js)

**Используем: Prisma Migrate** ✅

**Почему:**
```typescript
// Next.js использует Prisma ORM (не SQLAlchemy)

// schema.prisma
model Post {
  id        Int      @id @default(autoincrement())
  title     String
  content   String?  // Новое поле!
  published Boolean  @default(false)
}

// Создать миграцию:
npx prisma migrate dev --name add_content_to_post

// Применить на production:
npx prisma migrate deploy
```

**Не используем Alembic:**
- ❌ Alembic для Python/SQLAlchemy
- ✅ Prisma Migrate для JavaScript/TypeScript

---

### 3. Интерактивная онлайн-школа (RAG + AI)

**Используем: Alembic + ручные миграции для pgvector** 🔄

**Почему:**
```python
# FastAPI Backend с PostgreSQL + pgvector extension

# Обычные таблицы → Alembic миграции:
class User(Base):
    pass

class Course(Base):
    pass

# Специальная таблица с векторами → Alembic + ручной SQL:
class Document(Base):
    embedding = Column(Vector(1536))  # pgvector type!

# Миграция для pgvector:
# alembic/versions/xxx_add_pgvector.py
def upgrade():
    # Установить pgvector extension
    op.execute("CREATE EXTENSION IF NOT EXISTS vector")
    
    # Создать таблицу с vector column
    op.create_table(
        'documents',
        sa.Column('id', sa.Integer(), primary_key=True),
        sa.Column('content', sa.Text()),
        sa.Column('embedding', Vector(1536))  # Vector type!
    )
```

**Комбинация:**
- ✅ Alembic для обычных таблиц
- ✅ Ручной SQL в миграциях для pgvector extension

---

## 🔄 Жизненный цикл миграций

### Типичный workflow разработки

```
┌─────────────────────────────────────────────────────────────────┐
│                  ЖИЗНЕННЫЙ ЦИКЛ МИГРАЦИЙ                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1️⃣ ИЗМЕНЕНИЕ МОДЕЛИ                                            │
│     Разработчик меняет SQLAlchemy модель                        │
│     ├── models/project.py                                       │
│     └── + status = Column(String(50))  # Новое поле            │
│                           │                                     │
│                           ▼                                     │
│  2️⃣ СОЗДАНИЕ МИГРАЦИИ                                           │
│     alembic revision --autogenerate                             │
│     ├── Alembic сравнивает модели с БД                          │
│     ├── Генерирует код миграции                                 │
│     └── alembic/versions/xxx_add_status.py создан              │
│                           │                                     │
│                           ▼                                     │
│  3️⃣ ПРОВЕРКА МИГРАЦИИ                                           │
│     Разработчик проверяет сгенерированный код                   │
│     ├── def upgrade(): ...  # Применить изменения               │
│     └── def downgrade(): ... # Откатить изменения               │
│                           │                                     │
│                           ▼                                     │
│  4️⃣ ПРИМЕНЕНИЕ ЛОКАЛЬНО                                         │
│     alembic upgrade head                                        │
│     ├── Миграция применяется к локальной БД                     │
│     └── Таблица projects обновлена                              │
│                           │                                     │
│                           ▼                                     │
│  5️⃣ ТЕСТИРОВАНИЕ                                                │
│     pytest tests/                                               │
│     ├── Проверка что всё работает                               │
│     └── Тесты проходят ✅                                        │
│                           │                                     │
│                           ▼                                     │
│  6️⃣ КОММИТ В GIT                                                │
│     git add alembic/versions/xxx_add_status.py                  │
│     git commit -m "Add status field to projects"               │
│                           │                                     │
│                           ▼                                     │
│  7️⃣ DEPLOY НА PRODUCTION                                        │
│     git push origin main                                        │
│     ├── CI/CD pipeline запускается                              │
│     ├── alembic upgrade head  # Автоматически!                  │
│     └── Production БД обновлена ✅                               │
│                                                                 │
│  💡 Если проблема → alembic downgrade -1  # Откат              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📚 Alembic основы

### Что такое Alembic?

**Alembic** - это инструмент миграций для SQLAlchemy. Создан тем же автором что и SQLAlchemy.

**Особенности:**
- ✅ Автогенерация миграций (сравнивает модели с БД)
- ✅ Version control (история миграций)
- ✅ Upgrade / Downgrade (применить / откатить)
- ✅ Git-friendly (файлы миграций в репозитории)

### Установка Alembic

```bash
pip install alembic
```

### Инициализация Alembic

```bash
# В папке backend/
alembic init alembic
```

**Создаётся структура:**
```
backend/
├── alembic/
│   ├── versions/          # Папка с миграциями (будут здесь)
│   ├── env.py             # Настройки окружения
│   ├── script.py.mako     # Шаблон миграции
│   └── README
├── alembic.ini            # Конфигурация Alembic
├── app/
│   ├── database.py
│   └── models/
└── ...
```

---

### Настройка Alembic

#### Файл: `alembic.ini`

```ini
# Указываем URL БД через переменную окружения
sqlalchemy.url = driver://user:pass@localhost/dbname

# ИЛИ используем переменную из .env (лучше!)
# sqlalchemy.url = 
```

#### Файл: `alembic/env.py`

```python
from logging.config import fileConfig
from sqlalchemy import engine_from_config, pool
from alembic import context
import os
import sys

# Добавляем путь к app для импорта моделей
sys.path.insert(0, os.path.realpath(os.path.join(os.path.dirname(__file__), '..')))

# Импортируем Base и модели
from app.database import Base
from app.models import User, Project, Cam  # Импорт всех моделей!

# Alembic Config объект
config = context.config

# Получаем DATABASE_URL из .env
from dotenv import load_dotenv
load_dotenv()
config.set_main_option("sqlalchemy.url", os.getenv("DATABASE_URL"))

# Устанавливаем target_metadata для автогенерации
target_metadata = Base.metadata

def run_migrations_offline():
    """Миграции в offline режиме (генерация SQL)"""
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )

    with context.begin_transaction():
        context.run_migrations()

def run_migrations_online():
    """Миграции в online режиме (применение к БД)"""
    connectable = engine_from_config(
        config.get_section(config.config_ini_section),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )

    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=target_metadata
        )

        with context.begin_transaction():
            context.run_migrations()

# Выбираем режим
if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

---

### Создание первой миграции

```bash
# Создать initial миграцию (создание всех таблиц)
alembic revision --autogenerate -m "Initial migration"

# Alembic:
# 1. Смотрит на модели в app/models/
# 2. Смотрит на БД (пока пустая)
# 3. Генерирует миграцию "создать все таблицы"
```

**Создаётся файл:**
```
alembic/versions/abc123_initial_migration.py
```

**Содержимое:**
```python
"""Initial migration

Revision ID: abc123
Revises: 
Create Date: 2025-10-12 10:30:45.123456
"""
from alembic import op
import sqlalchemy as sa

# Revision identifiers
revision = 'abc123'
down_revision = None
branch_labels = None
depends_on = None

def upgrade():
    """Применить миграцию (создать таблицы)"""
    # Создать таблицу users
    op.create_table(
        'users',
        sa.Column('id', sa.String(255), primary_key=True),
        sa.Column('email', sa.String(255), nullable=False),
        sa.Column('subscription_status', sa.String(50)),
        sa.Column('created_at', sa.DateTime()),
        sa.Column('updated_at', sa.DateTime())
    )
    
    # Создать индекс на email
    op.create_index('ix_users_email', 'users', ['email'], unique=True)
    
    # Создать таблицу projects
    op.create_table(
        'projects',
        sa.Column('id', sa.Integer(), primary_key=True),
        sa.Column('user_id', sa.String(255), sa.ForeignKey('users.id')),
        sa.Column('name', sa.String(255), nullable=False),
        sa.Column('description', sa.Text()),
        sa.Column('created_at', sa.DateTime()),
        sa.Column('updated_at', sa.DateTime())
    )
    
    # ... и так далее для cams

def downgrade():
    """Откатить миграцию (удалить таблицы)"""
    op.drop_table('cams')
    op.drop_table('projects')
    op.drop_table('users')
```

---

### Применение миграции

```bash
# Применить все непримененные миграции
alembic upgrade head

# Вывод:
INFO  [alembic.runtime.migration] Running upgrade  -> abc123, Initial migration
```

**Что произошло:**
- ✅ Таблицы созданы в БД
- ✅ Alembic создал таблицу `alembic_version` (хранит текущую версию)
- ✅ В `alembic_version` записано: `version = 'abc123'`

---

### Создание последующих миграций

**Сценарий:** Добавляем поле `status` в модель `Project`

```python
# app/models/project.py
class Project(Base):
    __tablename__ = "projects"
    
    id = Column(Integer, primary_key=True)
    user_id = Column(String(255), ForeignKey("users.id"))
    name = Column(String(255), nullable=False)
    description = Column(Text)
    status = Column(String(50), default="active")  # НОВОЕ ПОЛЕ!
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow)
```

**Создать миграцию:**
```bash
alembic revision --autogenerate -m "Add status to projects"
```

**Alembic генерирует:**
```python
# alembic/versions/def456_add_status_to_projects.py
def upgrade():
    """Добавить колонку status"""
    op.add_column('projects', sa.Column('status', sa.String(50)))

def downgrade():
    """Удалить колонку status"""
    op.drop_column('projects', 'status')
```

**Применить:**
```bash
alembic upgrade head
```

**Откатить (если нужно):**
```bash
alembic downgrade -1  # Откатить одну миграцию назад
```

---

## 🤖 Работа с Claude Code

### Задача: Настроить Alembic для EngineCamPro v2

---

### ❌ Плохой диалог (неэффективный)

```
Ты: Настрой миграции для проекта.

Claude Code: Какой инструмент использовать? Какая БД?

Ты: PostgreSQL.

Claude Code: Alembic? Django migrations? Какой проект?

Ты: Не знаю. Выбери что-нибудь.

Claude Code: [создаёт базовую настройку, может не подойти]
```

**Проблемы:**
- ❌ Нет контекста проекта
- ❌ Не указаны модели
- ❌ Неясна структура БД
- ❌ Claude Code вынужден угадывать

---

### ✅ Хороший диалог (эффективный)

```
Ты: Настрой Alembic миграции для EngineCamPro v2.

Контекст проекта:
- Backend: FastAPI (async SQLAlchemy)
- База данных: PostgreSQL (Supabase)
- ORM: SQLAlchemy 2.0+
- Python 3.11+
- Модели уже созданы в app/models/

Структура проекта:
backend/
├── app/
│   ├── database.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── project.py
│   │   └── cam.py
│   └── ...
├── .env (DATABASE_URL здесь)
└── ... (здесь создать alembic/)

Требования:
1. Инициализировать Alembic в backend/
2. Настроить alembic/env.py:
   - Импорт моделей из app.models
   - Чтение DATABASE_URL из .env
   - target_metadata = Base.metadata
3. Настроить alembic.ini (убрать hardcoded URL)
4. Создать initial миграцию (создание всех таблиц)
5. Добавить .gitignore для __pycache__ в alembic/

Best practices:
- Async SQLAlchemy compatible
- Автогенерация миграций (--autogenerate)
- Понятные названия миграций
- Downgrade функции для отката

Claude Code: Отлично! Детальный план настройки Alembic:

1. Установка:
   ```bash
   pip install alembic python-dotenv
   ```

2. Инициализация:
   ```bash
   cd backend
   alembic init alembic
   ```

3. Настройка alembic/env.py:
   - Добавлю sys.path для импорта app
   - Импорт Base и всех моделей
   - Чтение DATABASE_URL из .env
   - target_metadata = Base.metadata
   - run_migrations_online() для async

4. Настройка alembic.ini:
   - Уберу hardcoded sqlalchemy.url
   - Будем читать из .env

5. Создание initial миграции:
   ```bash
   alembic revision --autogenerate -m "Initial migration: create users, projects, cams"
   ```

6. .gitignore обновлю:
   ```
   alembic/versions/__pycache__/
   alembic/__pycache__/
   ```

Применить? [Y/n]

Ты: Y

Claude Code: ✅ Настроил Alembic!

backend/alembic/env.py:
```python
import os
import sys
from logging.config import fileConfig
from sqlalchemy import engine_from_config, pool
from alembic import context
from dotenv import load_dotenv

# Добавляем путь к app
sys.path.insert(0, os.path.realpath(os.path.join(os.path.dirname(__file__), '..')))

# Импортируем Base и модели
from app.database import Base
from app.models import User, Project, Cam

# Alembic Config
config = context.config

# Читаем DATABASE_URL из .env
load_dotenv()
database_url = os.getenv("DATABASE_URL")
config.set_main_option("sqlalchemy.url", database_url)

# target_metadata для автогенерации
target_metadata = Base.metadata

# ... run_migrations_online() и run_migrations_offline()
```

backend/alembic.ini:
```ini
[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

# URL читается из .env в env.py
# sqlalchemy.url =

[loggers]
keys = root,sqlalchemy,alembic
...
```

backend/.gitignore (обновлён):
```
__pycache__/
*.pyc
.env
venv/
alembic/versions/__pycache__/
alembic/__pycache__/
```

Теперь создам initial миграцию:
```bash
alembic revision --autogenerate -m "Initial migration: create users, projects, cams"
```

Создан файл: alembic/versions/abc123_initial_migration.py

Проверь содержимое:
```bash
cat alembic/versions/abc123_initial_migration.py
```

Применить миграцию к БД? [Y/n]

Ты: Y

Claude Code: Применяю миграцию...
```bash
alembic upgrade head
```

INFO  [alembic.runtime.migration] Running upgrade  -> abc123, Initial migration

✅ Миграция применена!

Проверь в Supabase Dashboard:
- Table Editor → должны быть таблицы: users, projects, cams, alembic_version

alembic_version содержит текущую версию схемы: 'abc123'

Готово! Теперь при изменении моделей просто:
1. Измени модель в app/models/
2. alembic revision --autogenerate -m "Description"
3. alembic upgrade head

Хочешь создать тестовую миграцию (добавить поле status)? [Y/n]

Ты: Да! Покажи workflow изменения схемы.

Claude Code: Отлично! Демонстрирую полный workflow:

**Шаг 1:** Изменяю модель Project

app/models/project.py:
```python
class Project(Base):
    # ... существующие поля
    status = Column(String(50), default="active")  # НОВОЕ ПОЛЕ!
```

**Шаг 2:** Создаю миграцию
```bash
alembic revision --autogenerate -m "Add status field to projects"
```

Создан: alembic/versions/def456_add_status_field_to_projects.py

**Шаг 3:** Показываю содержимое миграции

```python
"""Add status field to projects

Revision ID: def456
Revises: abc123
"""
from alembic import op
import sqlalchemy as sa

def upgrade():
    op.add_column('projects', sa.Column('status', sa.String(50), server_default='active'))

def downgrade():
    op.drop_column('projects', 'status')
```

**Шаг 4:** Применяю миграцию
```bash
alembic upgrade head
```

INFO  [alembic.runtime.migration] Running upgrade abc123 -> def456, Add status field to projects

✅ Поле status добавлено в таблицу projects!

Проверяю в БД:
```sql
SELECT * FROM alembic_version;
-- version = 'def456'

DESCRIBE projects;
-- ... existing columns ...
-- status | character varying(50) | default 'active'
```

**Если нужно откатить:**
```bash
alembic downgrade -1
```

INFO  [alembic.runtime.migration] Running downgrade def456 -> abc123

✅ Поле status удалено!

**Workflow в команде:**

1. Developer A:
   ```bash
   # Изменил модель
   # Создал миграцию
   alembic revision --autogenerate -m "Add status"
   git add alembic/versions/def456_add_status.py
   git commit -m "Add project status field"
   git push
   ```

2. Developer B:
   ```bash
   git pull
   # Получил новую миграцию
   alembic upgrade head  # Применил к своей БД
   # Теперь схема БД синхронизирована!
   ```

3. CI/CD (Production):
   ```yaml
   - name: Apply migrations
     run: alembic upgrade head
   ```

Всё! Миграции настроены и работают. 🎉
```

**Почему это эффективно:**
- ✅ Детальный контекст проекта
- ✅ Чёткие требования к настройке
- ✅ Итеративное улучшение (initial → изменение)
- ✅ Показан полный workflow (dev → team → CI/CD)
- ✅ Claude Code создал всю инфраструктуру миграций

---

## 🛠️ Практическое задание

### Задача

Настрой Alembic миграции для EngineCamPro v2 через диалог с Claude Code. Создай initial миграцию и примени её к Supabase PostgreSQL.

---

### Работа с Claude Code

**Шаг 1:** Открой терминал в папке backend

```bash
cd ~/projects/enginecampro-v2/backend
claude
```

---

**Шаг 2:** Диалог с Claude Code

Скопируй диалог из секции "✅ Хороший диалог" выше и общайся с Claude Code. Он:
- Инициализирует Alembic
- Настроит `alembic/env.py`
- Обновит `alembic.ini`
- Создаст initial миграцию
- Применит миграцию к Supabase

---

**Шаг 3:** Проверка результата

```bash
# Проверь что миграция применена
alembic current

# Вывод:
abc123 (head)
```

**В Supabase Dashboard:**
1. Зайди в Table Editor
2. Должны быть таблицы: `users`, `projects`, `cams`, `alembic_version`
3. Открой таблицу `alembic_version` - должна быть версия `abc123`

---

**Шаг 4:** Создай тестовую миграцию

```bash
# Попроси Claude Code изменить модель Project (добавить поле status)
# Затем создать и применить миграцию

alembic revision --autogenerate -m "Add status to projects"
alembic upgrade head

# Проверь что поле добавлено
```

---

## ❓ Вопросы для самопроверки

### 1. Объясни что такое миграции БД через аналогию

Придумай свою аналогию (не обязательно Google Docs). Например: миграции как...?

<details>
<summary>💡 Подсказка</summary>

Подумай про:
- Версии программы (v1.0, v1.1, v2.0)
- Обновления операционной системы
- Ремонт квартиры (поэтапный)
- Эволюция вида
</details>

---

### 2. Зачем нужны миграции? Перечисли минимум 5 преимуществ

1. _______
2. _______
3. _______
4. _______
5. _______

<details>
<summary>💡 Подсказка</summary>

Подумай про:
- Безопасность данных (не потерять)
- Команда (синхронизация)
- История изменений
- Откат (rollback)
- Автоматизация (CI/CD)
</details>

---

### 3. Что произойдёт если изменить модель SQLAlchemy БЕЗ миграции?

Опиши сценарий:
1. У тебя есть таблица `projects` с 100 записями
2. Ты добавляешь поле `status` в модель
3. Что будет при запросе `session.query(Project).all()`?

<details>
<summary>💡 Подсказка</summary>

Подумай про:
- Модель в коде vs схема в БД
- SQLAlchemy ожидает поле status
- PostgreSQL не знает про это поле
- Что вернёт SELECT * FROM projects?
</details>

---

### 4. Alembic команды - что они делают?

Объясни своими словами:
- `alembic revision --autogenerate -m "Message"`
- `alembic upgrade head`
- `alembic downgrade -1`
- `alembic current`

<details>
<summary>💡 Подсказка</summary>

revision - создание
upgrade - применение
downgrade - откат
current - текущая версия
</details>

---

### 5. Workflow в команде - опиши шаги

Разработчик A добавил поле `description` в модель `Cam`.
Что должны сделать:
1. Разработчик A
2. Разработчик B (коллега)
3. CI/CD (на production)

<details>
<summary>💡 Подсказка</summary>

A: Создать миграцию, закоммитить в Git
B: git pull, применить миграцию локально
CI/CD: автоматически применить миграцию на production
</details>

---

## 🔗 Связь с другими уроками

**Основано на уроках:**
- **Урок 3.2:** PostgreSQL и SQLAlchemy ORM - создали модели, теперь управляем ими через миграции
- **Модуль 2:** Backend разработка - FastAPI endpoints будут использовать мигрированную схему БД

**Подготавливает к урокам:**
- **Урок 3.4:** Supabase - уже применили миграции к Supabase PostgreSQL
- **Модуль 5:** Интеграция - при изменении API нужно менять схему БД через миграции
- **Модуль 7:** Deploy - CI/CD будет автоматически применять миграции

**Связь с EngineCamPro:**
- Этот урок создал **систему управления изменениями БД**
- Теперь можем безопасно эволюционировать схему БД
- В production миграции применяются автоматически через CI/CD

**Связь с Next.js (Модуль 8):**
- В Модуле 8 увидишь Prisma Migrate для JavaScript/TypeScript
- Та же концепция миграций, но для другого ORM

**Связь с RAG (Модуль 9):**
- В Модуле 9 создадим миграцию для pgvector extension через Alembic

---

## ✅ Критерии завершения урока

**Понимание (могу объяснить):**
- [ ] Что такое миграции БД и зачем они нужны
- [ ] Проблему изменения схемы БД без миграций
- [ ] Как Alembic работает (autogenerate, upgrade, downgrade)
- [ ] Workflow миграций в команде (Git + Alembic)
- [ ] Разница между upgrade и downgrade

**Практические навыки:**
- [ ] Настроил Alembic через Claude Code
- [ ] Создал initial миграцию (все таблицы)
- [ ] Применил миграцию к Supabase PostgreSQL
- [ ] Создал тестовую миграцию (добавление поля)
- [ ] Умею откатывать миграции (downgrade)

**Готовность к следующему уроку:**
- [ ] Alembic настроен и работает
- [ ] Миграции применяются к БД
- [ ] Понимаю полный lifecycle миграций
- [ ] Готов изучать Supabase возможности

**Дополнительно:**
- [ ] Задокументировал выводы в `notes/module-3/lesson-3-3-notes.md`
- [ ] Ответил на все вопросы для самопроверки
- [ ] Закоммитил миграции в Git

---

## 📚 Дополнительные материалы (для углубления)

Если хочешь узнать больше:

**Документация:**
- [Alembic официальная документация](https://alembic.sqlalchemy.org/) - полное руководство
- [SQLAlchemy + Alembic tutorial](https://alembic.sqlalchemy.org/en/latest/tutorial.html) - пошаговый tutorial
- [Database Migrations Best Practices](https://www.prisma.io/dataguide/types/relational/migrations) - общие принципы

**Статьи:**
- "Understanding Database Migrations" - концепция миграций
- "Alembic Auto-generate Limitations" - когда autogenerate не работает

**Видео (опционально):**
- "Database Migrations Explained" - общая концепция
- "Alembic Tutorial for SQLAlchemy" - детальный гайд

**НО!** Это не обязательно для курса. Главное - понять концепцию миграций и уметь использовать Alembic через Claude Code.

---

**Статус урока:** ⏳ Не начат  
**Дата начала:** _________  
**Дата завершения:** _________  
**Время потрачено:** _______ минут

---

## 🎉 Поздравляю!

Ты теперь понимаешь как безопасно эволюционировать схему базы данных через миграции! Это критически важный навык для production приложений.

**В следующем уроке (3.4)** мы завершим Модуль 3 изучением Supabase - Backend-as-a-Service платформы, которая предоставляет PostgreSQL + Auth + Storage + Real-time из коробки.

**Помни:** Миграции = Version Control для БД. Git для кода, Alembic для схемы! 🚀